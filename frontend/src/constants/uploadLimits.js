/** Keep in sync with backend `FILE_SIZE_LIMITS.pdf` in `cloudinaryConfig.js`. */
export const MAX_CLOUDINARY_PDF_BYTES = 10 * 1024 * 1024;

export const maxPdfSizeLabelMb = MAX_CLOUDINARY_PDF_BYTES / (1024 * 1024);
