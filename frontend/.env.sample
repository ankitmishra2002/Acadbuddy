# Full base URL for the backend API (must include the /api prefix used by Express mounts).
# Local example:
VITE_API_URL=http://localhost:7000/api

# Vercel: set the same variable in Project Settings → Environment Variables for Production and Preview, e.g.:
# VITE_API_URL=https://your-service-name.onrender.com/api
